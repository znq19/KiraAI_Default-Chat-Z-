{
  "waking_words": {
    "name": "Waking words",
    "type": "list",
    "default": [],
    "hint": "Mark as mentioned if any of the words appeared in the message"
  },
  "receive_unmentioned": {
    "name": "Receive Unmentioned Messages",
    "type": "switch",
    "default": false,
    "hint": "Receive unmentioned messages as context"
  },
  "max_unmentioned_messages": {
    "name": "Max Unmentioned Messages",
    "type": "integer",
    "default": 5,
    "hint": "Max unmentioned message count to be added to buffer as context"
  },
  "group_chat_prompt": {
    "name": "Group Chat Prompt",
    "type": "markdown",
    "default": "",
    "hint": "Extra prompt for AI to better behave in group chat"
  },
  "group_proactive_chat": {
    "name": "Group Proactive Chat",
    "type": "switch",
    "default": false,
    "hint": "Allow AI to send messages proactively in group chats, be sure to have receive_unmentioned enabled"
  },
  "group_proactive_chat_probability": {
    "name": "Group Proactive Chat Probability",
    "type": "float",
    "default": 0.1,
    "hint": "Probability of triggering a proactive message (default 0.1)"
  },
  "image_recognition_only_on_mention": {
    "name": "仅唤醒消息识图",
    "type": "switch",
    "default": true,
    "hint": "为true时，只有唤醒消息（@或唤醒词）中的图片/动画表情会保留（识别）；非唤醒消息中的全部替换为文本。为false时，所有消息中的图片/表情按概率和数量处理。"
  },
  "image_recognition_probability": {
    "name": "识图概率",
    "type": "number",
    "default": 0.5,
    "minimum": 0,
    "maximum": 1,
    "hint": "非唤醒消息中图片/表情被保留的概率（仅在image_recognition_only_on_mention=false时生效）。"
  },
  "max_images_per_message": {
    "name": "最多保留图片/表情数",
    "type": "integer",
    "default": 3,
    "minimum": 0,
    "hint": "每条消息最多保留的图片+动画表情数量（超出部分替换为文本）。仅在image_recognition_only_on_mention=false时生效。"
  },
  "forward_recognition_only_on_mention": {
    "name": "仅唤醒消息解析转发",
    "type": "switch",
    "default": true,
    "hint": "为true时，只有唤醒消息中的合并转发消息会被展开显示详细内容；非唤醒消息中的转发消息替换为[转发消息]。为false时，所有转发消息都展开（注意：可能消耗较多token）。"
  },
  "voice_recognition_only_on_mention": {
    "name": "仅唤醒消息识别语音（群聊）",
    "type": "switch",
    "default": true,
    "hint": "为true时，只有唤醒消息中的语音会尝试识别；非唤醒消息中的语音替换为[语音]。"
  },
  "voice_private_need_mention": {
    "name": "私聊需要引用才识别语音",
    "type": "switch",
    "default": true,
    "hint": "为true时，私聊中只有引用机器人（回复）的语音才尝试识别；为false时，私聊中所有语音都尝试识别（需STT模型）。"
  },
  "voice_max_duration": {
    "name": "语音最大识别时长（秒）",
    "type": "integer",
    "default": 0,
    "minimum": 0,
    "hint": "仅当语音时长不超过此值时才会尝试识别（0表示不限制）。超过此秒数的语音会直接替换为[长语音 X秒]。"
  }
}
